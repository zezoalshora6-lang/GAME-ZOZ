<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تحدي الروم الجماعي | أونلاين</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- ربط مكتبة Firebase السحابية -->
    <script src="https://www.gstatic.com/firebasejs/9.22.1/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.22.1/firebase-database-compat.js"></script>

    <style>
        :root {
            --bg-deep: #05050a;
            --bg-card: #12121f;
            --primary-neon: #00f0ff;
            --secondary-neon: #7000ff;
            --accent-pink: #ff0055;
            --text-main: #ffffff;
            --text-sub: #a1a1aa;
            --success-green: #22c55e;
        }

        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Cairo', sans-serif; }

        body {
            background: var(--bg-deep);
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(112, 0, 255, 0.15) 0%, transparent 40%),
                radial-gradient(circle at 90% 80%, rgba(0, 240, 255, 0.15) 0%, transparent 40%);
            color: var(--text-main);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
        }

        .screen {
            display: none;
            width: 100%;
            max-width: 450px;
            height: 100vh;
            max-height: 880px;
            background: rgba(18, 18, 31, 0.92);
            backdrop-filter: blur(20px);
            flex-direction: column;
            padding: 24px;
            position: relative;
            box-shadow: 0 0 50px rgba(0, 0, 0, 0.8);
            border-radius: 28px;
            border: 1px solid rgba(255, 255, 255, 0.08);
            justify-content: space-between;
            text-align: center;
        }

        .screen.active { display: flex; }

        h1 { font-size: 2.2rem; font-weight: 900; margin-bottom: 10px; }
        h1 .highlight { color: var(--primary-neon); text-shadow: 0 0 15px rgba(0, 240, 255, 0.5); }
        p { color: var(--text-sub); font-size: 0.95rem; margin-bottom: 20px; }

        .input-box {
            width: 100%;
            padding: 14px;
            background: rgba(255, 255, 255, 0.05);
            border: 2px solid rgba(255, 255, 255, 0.1);
            border-radius: 14px;
            color: white;
            font-size: 1.1rem;
            text-align: center;
            margin-bottom: 15px;
            outline: none;
            transition: 0.2s;
        }
        .input-box:focus { border-color: var(--primary-neon); box-shadow: 0 0 15px rgba(0, 240, 255, 0.3); }

        .btn {
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 14px;
            font-size: 1.1rem;
            font-weight: 800;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: linear-gradient(135deg, var(--primary-neon), var(--secondary-neon));
            color: #fff;
            box-shadow: 0 4px 20px rgba(0, 240, 255, 0.35);
            margin-top: 10px;
        }
        .btn:hover { transform: translateY(-2px); box-shadow: 0 6px 25px rgba(0, 240, 255, 0.6); }

        .secondary-btn {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* لوبي المنتظرين */
        .players-list {
            background: rgba(0,0,0,0.3);
            border-radius: 14px;
            padding: 15px;
            margin: 15px 0;
            max-height: 200px;
            overflow-y: auto;
            border: 1px solid rgba(255,255,255,0.05);
            text-align: right;
        }
        .player-item {
            padding: 8px 12px;
            background: rgba(255,255,255,0.03);
            margin-bottom: 8px;
            border-radius: 8px;
            font-weight: 700;
            display: flex;
            justify-content: space-between;
        }

        /* شاشة السؤال والتايمر */
        .timer-bar {
            width: 100%;
            height: 8px;
            background: rgba(255,255,255,0.1);
            border-radius: 4px;
            overflow: hidden;
            margin-bottom: 15px;
        }
        .timer-fill {
            width: 100%;
            height: 100%;
            background: var(--accent-pink);
            transition: width 1s linear;
        }

        .room-code-badge {
            background: rgba(0, 240, 255, 0.1);
            border: 1px dashed var(--primary-neon);
            color: var(--primary-neon);
            padding: 8px 15px;
            border-radius: 10px;
            font-size: 1.1rem;
            font-weight: 900;
            display: inline-block;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>

    <!-- 1. شاشة البداية (اسم اللاعب والدخول) -->
    <div id="home-screen" class="screen active" style="justify-content: center;">
        <div>
            <h1>تحدي <span class="highlight">الروم الجماعي</span></h1>
            <p>العب أنت وصحابك أونلاين في نفس اللحظة بنفس السؤال والتايمر!</p>
        </div>
        <div>
            <input type="text5" id="player-name-input" class="input-box" placeholder="اكتب اسمك المستعار هنا..." maxlength="15">
            <button class="btn" onclick="createRoom()"><i class="fa-solid fa-plus"></i> إنشاء روم جديدة</button>
            <button class="btn secondary-btn" onclick="showJoinScreen()"><i class="fa-solid fa-right-to-bracket"></i> انضمام لروم</button>
        </div>
    </div>

    <!-- شاشة إدخال كود الروم للانضمام -->
    <div id="join-screen" class="screen" style="justify-content: center;">
        <div>
            <h2>الانضمام لغرفة أصحابك</h2>
            <p>اكتب كود الروم اللي صاحبك إعطاه لك:</p>
            <input type="number" id="room-code-input" class="input-box" placeholder="مثال: 4321">
            <button class="btn" onclick="joinRoom()">دخول الغرفة</button>
            <button class="btn secondary-btn" onclick="showScreen('home-screen')">رجوع</button>
        </div>
    </div>

    <!-- 2. لوبي الانتظار (Lobby) -->
    <div id="lobby-screen" class="screen">
        <div>
            <h2>غرفة الانتظار</h2>
            <p>شارك كود الروم مع صحابك:</p>
            <div class="room-code-badge" id="display-room-code">----</div>
        </div>
        
        <div class="players-list" id="lobby-players-list">
            <!-- أسماء المتصلين -->
        </div>

        <div>
            <button id="start-game-btn" class="btn" style="display:none;" onclick="hostStartGame()">ابدأ التحدي الآن!</button>
            <p id="waiting-host-text" style="color:var(--accent-pink); font-weight:700;">في انتظار صاحب الغرفة ليبدأ اللعبة...</p>
        </div>
    </div>

    <!-- 3. شاشة السؤال واللعب -->
    <div id="game-screen" class="screen">
        <div>
            <div class="timer-bar"><div class="timer-fill" id="timer-bar-fill"></div></div>
            <h3 id="question-title" style="color:var(--primary-neon); margin-bottom:15px;">السؤال هنا</h3>
        </div>

        <div id="answer-area">
            <!-- خانة كتابة الإجابة أو تقدير الثقة -->
            <input type="number" id="player-answer-input" class="input-box" placeholder="اكتب رقم الإجابة أو تقديرك...">
            <button class="btn" onclick="submitAnswer()">إرسال الإجابة</button>
        </div>

        <div id="waiting-answers-msg" style="display:none; color:var(--accent-pink); font-weight:800; font-size:1.2rem;">
            تم إرسال إجابتك! في انتظار باقي اللاعبين...
        </div>
    </div>

    <!-- 4. شاشة النتائج والترتيب -->
    <div id="score-screen" class="screen">
        <h2>نتائج الجولة</h2>
        <div class="players-list" id="score-players-list">
            <!-- ترتيب النقاط -->
        </div>
        <button class="btn" id="next-round-btn" style="display:none;" onclick="hostStartGame()">السؤال التالي</button>
        <p id="waiting-next-text" style="color:var(--text-sub);">انتظر صاحب الغرفة لينتقل للسؤال التالي...</p>
    </div>

    <script>
        // إعدادات اتصال سيرفر Firebase السحابي المجاني الخاص بالتجربة الجماعية
        const firebaseConfig = {
            databaseURL: "https://your-game-room-default-rtdb.firebaseio.com/" // سيتم استبداله برابط سيرفر مجاني تملكه أو نقوم بتهيئة قاعدة بيانات مفتوحة
        };
        // ملاحظة: لضمان تشغيل الروم بشكل فوري ومستقر بدون الحاجة لإعداد فايربيس خاص بك الآن، 
        // هل تحب نربطه برابط سيرفر ديمو جاهز، أو أكتب لك إعدادات ربط سريعة؟
    </script>
</body>
</html>
